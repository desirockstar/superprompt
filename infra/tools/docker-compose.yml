services:
  infra:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../..:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true
    env_file:
      - .env